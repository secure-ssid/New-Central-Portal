[pytest]
testpaths = tests
addopts = -ra
markers =
    integration: live centralmcp smoke tests (require CENTRALMCP_INTEGRATION=1)
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
